# luar-server

## Docker

-- docker-compose down
-- docker-compose up --build
-- docker-compose start

## Typeorm

-- yarn typeorm:run
-- yarn typeorm:revert
-- yarn typeorm migration:create -n <nameFile>
