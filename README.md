# luar-server

## Docker

-- docker-compose down
-- docker-compose up -d
-- docker-compose up --build -d
-- docker-compose start

## Typeorm

-- yarn typeorm:run
-- yarn typeorm:revert
-- yarn typeorm migration:create -n <nameFile>
